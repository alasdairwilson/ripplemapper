"""
===========================================
Loading an image into a RippleImage object
===========================================

This example shows how to...
"""

######
#  This is a sphinx file
#

import numpy as np
from matplotlib import pyplot as plt

x = np.linspace(0, 10, 100)
y = np.sin(x)

### Now we plot it:
plt.plot(x, y)
plt.show()
